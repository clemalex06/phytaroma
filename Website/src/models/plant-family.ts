export default class PlantFamily {
    public id: string;
    public name: string;
    public link: string;
    constructor(id: string, name: string, link: string) {
        this.id = id;
        this.name = name;
        this.link = link;
    }
}